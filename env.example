# Detect app environment variables.
# Copy to .env.local for local development.

# ---- Database ----
# Use a separate logical DB from manager. Locally, the simplest is a sibling DB
# on the same MySQL instance, e.g. mysql://root:pass@127.0.0.1:3306/detect
DATABASE_URL=mysql://root:password@127.0.0.1:3306/detect

# ---- NextAuth ----
NEXTAUTH_SECRET=replace-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# ---- Google OAuth ----
# Configure at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI:  http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ---- Privacy / hashing ----
# Used to hash client IPs before storing them. Rotate at most once a year.
IP_HASH_SALT=replace-with-openssl-rand-base64-32

# ---- Optional: ipinfo.io free token (for ASN / VPN / proxy detection) ----
# IPINFO_TOKEN=

# ---- Optional: Email login (Mailgun) ----
# Without these, magic-link sign-in still works locally — the link is printed
# to the dev server console. In production, missing these will refuse to send
# (we never silently no-op a login attempt).
# MAILGUN_API_KEY=
# MAILGUN_DOMAIN=mg.example.com
# MAILGUN_REGION=us           # or 'eu'
# EMAIL_FROM=Leakish <noreply@example.com>

# ---- Stripe (subscriptions, optional) ----
# Set these to enable the /pricing + /account/billing flow. See README's
# "Billing & subscriptions" section for one-time Dashboard config (Products,
# Prices, Webhook endpoint, Customer Portal).
#
# Prefer a Restricted API Key (rk_test_… / rk_live_…) over a secret key.
# Each unset Price ID disables only that (plan, interval) cell in the
# pricing UI; the rest of the app keeps working.
# STRIPE_SECRET_KEY=rk_test_replace_me
# STRIPE_WEBHOOK_SECRET=whsec_replace_me  # from `stripe listen` in dev, or Dashboard in prod
# STRIPE_PRICE_STARTER_MONTHLY=price_replace_me
# STRIPE_PRICE_STARTER_YEARLY=price_replace_me
# STRIPE_PRICE_PRO_MONTHLY=price_replace_me
# STRIPE_PRICE_PRO_YEARLY=price_replace_me
# Optional: app URL for Stripe Checkout / Portal redirects. Falls back to NEXTAUTH_URL.
# NEXT_PUBLIC_APP_URL=https://your-app.example.com
